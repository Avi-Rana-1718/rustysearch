Insert document -> Token document fields -> store as inverted index eg hi -> d1, d3, avi -> d1


For single word search hashmap will do the trick but for multiple word search, need to use prefix tree

For incomplete word search need to implemnet even better tree